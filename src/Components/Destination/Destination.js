import React from "react";

// Styling
import styled from "styled-components";

const StyledDestination = styled.div`
  color: white;
`;

export default function Destination() {
  return <StyledDestination>Destination page</StyledDestination>;
}
